export enum StoragePath {
  ingredients = 'ingredients',
  recipes = 'recipes',
  settings = 'settings',
}

export enum Units {
  pieces = 'pcs',
  g = 'g',
  ml = 'ml',
}

export enum Mode {
  list,
  edit,
  view,
}